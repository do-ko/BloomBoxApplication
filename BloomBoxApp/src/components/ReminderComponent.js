import React, { useState } from "react";
import {
  Dimensions,
  Image,
  Pressable,
  StyleSheet,
  Text,
  View,
} from "react-native";

const ReminderComponent = (props) => {
  return (
    <View>
      <Text>This is a remider.</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ReminderComponent;
